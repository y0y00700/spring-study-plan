# 확인된 오개념

틀린 답을 감추지 않고, 처음 생각과 수정된 이해를 함께 기록합니다.

| 날짜 | 처음 생각 | 반례 또는 실험 | 수정된 이해 | 복습일 |
|---|---|---|---|---|
| 2026-07-23 | 사용하는 쪽에 `OrderService`를 주입하면 `OrderService` 내부 의존 객체의 생성 책임도 분리된다고 생각함 | `OrderService` 내부에 `new KakaoPaymentProcessor()`가 그대로 남아 있으면 구현체 변경 시 `OrderService`를 수정해야 함 | 분리하려는 의존 객체인 `PaymentProcessor`를 `OrderService` 생성자 매개변수로 받아야 함 | 2026-07-26 |
| 2026-07-23 | 생성자 주입 문제를 `@Autowired` 필드 주입으로 풀거나, 필드에서 구현체를 만든 뒤 생성자에서 덮어써도 된다고 생각함 | 필드가 구체 구현체를 직접 생성하면 외부에서 의존성을 전달해도 생성 책임과 구체 구현체 의존이 남음 | 구체 구현체 생성은 외부 조립 코드가 담당하고, 사용하는 객체는 생성자로 받은 의존성을 한 번 저장함 | 2026-07-26 |
| 2026-07-23 | 두 구현체 검증 중 카카오 객체를 호출하면서도 `"NAVER"`가 반환될 것으로 예측함 | 생성자에 카카오 객체 참조를 전달한 뒤 원래 지역변수를 네이버 객체로 재대입해도 `OrderService`는 계속 카카오 객체를 호출함 | 변수 재대입은 그 변수의 참조만 바꾸며, 이미 생성자에 전달되어 필드에 저장된 참조까지 바꾸지 않는다. 검증문에서는 실제 객체 연결 관계를 추적해야 함 | 2026-07-26 |
| 2026-07-23 | Reflection으로 annotation 존재를 확인하면 annotation의 동작과 대상 메서드도 함께 실행된다고 생각함 | `isAnnotationPresent()`는 존재 여부만 반환하며 메서드를 호출하지 않음 | annotation은 조사 대상인 메타데이터이고, Reflection은 조사 수단이며, 실제 동작에는 별도의 처리·호출 주체가 필요함 | 2026-07-24 |
| 2026-07-26 | `ApplicationContext.getBean()`은 설정 클래스 분석 결과를 바탕으로 별도 객체를 반환하고, 내부 `BeanFactory.getBean()`은 자신이 만든 다른 객체를 반환한다고 생각함 | 두 경로로 조회한 `OrderService`를 `assertSame`으로 비교하자 같은 Singleton 참조가 반환됨 | 설정 분석 결과는 Bean 정의이며 `getBean()`의 결과는 Bean 객체다. `ApplicationContext.getBean()`은 내부 `BeanFactory`에 조회를 위임하고 같은 Singleton 저장소의 객체를 반환한다. | 2026-07-27 |
| 2026-07-26 | 주입 메서드의 매개변수 타입만 확인하면 동일 타입 후보가 두 개라는 사실까지 알 수 있다고 설명함 | `orderService(PaymentProcessor)`의 매개변수 타입과 `kakaoProcessor()`·`naverProcessor()`의 반환 타입을 분리해 추적함 | 주입 지점의 매개변수 타입은 검색 조건이고, 각 Bean 정의의 타입 정보가 후보를 판별하는 근거다. | 2026-07-27 |
| 2026-07-27 | `method.isAnnotationPresent()`가 클래스에 annotation이 있는지 확인한다고 설명함 | `Method` 객체로 `pay()`의 `@Tracked`를 조회하고 클래스 조회와 구분함 | `method.isAnnotationPresent()`는 해당 `Method`가 나타내는 메서드를 확인한다. 조회 대상은 호출 주체인 `AnnotatedElement`에 따라 달라진다. | 2026-07-30 |
| 2026-07-27 | `OrderService` 생성자가 호출되는 시점에 `PaymentProcessor` 후보 객체를 확인한다고 설명함 | 지연 등록한 `PaymentProcessor`와 `OrderService`의 생성 이벤트를 검증하자 `PaymentProcessor 생성 → OrderService 생성` 순서였음 | BeanFactory는 생성자 호출 전에 매개변수 타입을 확인하고 후보 Bean 정의를 선택한다. 선택된 객체를 생성하거나 조회한 뒤 그 참조를 전달하며 생성자를 호출한다. | 2026-07-30 |
| 2026-07-28 | `setBeanName()`까지 인스턴스화 과정이며, `this`를 사용할 수 있으면 인스턴스화가 끝났다고 생각함 | 생성자 안에서도 `this`를 사용할 수 있고 `setBeanName()`은 생성자 실행 뒤 이미 만들어진 객체에 호출됨 | 인스턴스화는 생성자 호출로 객체를 만드는 단계이며, `setBeanName()`은 이후 컨테이너 정보를 전달하는 Aware 콜백이다. | 2026-07-31 |
| 2026-07-28 | 필드 주입 의존성을 생성자에서 사용할 수 있다고 생각함 | 객체 할당 시 필드 값은 기본적으로 `null`이고 필드 주입은 생성자 실행 뒤 진행됨 | 생성자 실행 시 의존성이 필요하면 생성자 주입으로 먼저 확보된 참조를 인자로 받아야 한다. | 2026-07-31 |
| 2026-07-28 | 명시적인 `@ComponentScan`이 없으면 스캔 여부를 알 수 없다고 생각함 | `@SpringBootApplication`의 합성 annotation 구성을 확인함 | `@SpringBootApplication`은 `@ComponentScan`을 포함하며, 별도 지정이 없으면 애플리케이션 클래스의 패키지와 하위 패키지가 기본 범위다. | 2026-07-31 |
| 2026-07-28 | scan 범위 밖의 `@Component`도 등록되거나 include filter로 포함될 수 있다고 생각함 | base package 밖의 `@Component`와 include 조건 일치 클래스 모두 BeanDefinition으로 등록되지 않음을 검증함 | filter는 base package로 정한 탐색 범위 안에서만 적용되며 include filter도 탐색 범위를 확장하지 않는다. | 2026-07-31 |
| 2026-07-29 | `proxyBeanMethods=false`이면 메서드 매개변수로 의존성을 받아도 별도 객체가 생성되고, 객체 생성과는 관계없는 옵션이라고 생각함 | 직접 `member()`를 호출한 설정과 `team(Member member)`로 주입받은 설정의 동일성·생성 횟수를 비교함 | 이 옵션은 `@Bean` 메서드 간 직접 호출의 프록시 개입 여부를 정한다. 매개변수 의존성은 BeanFactory가 해결하므로 `false`여도 관리 객체가 전달된다. | 2026-08-01 |
| 2026-07-29 | `proxyBeanMethods=false`의 직접 호출에서 `Team` 생성자가 2회 실행된다고 설명함 | 컨테이너의 `Member`와 `team()` 내부 직접 호출로 만든 `Member`를 분리해 생성 횟수를 검증함 | `Member`가 2개 생성되고 `Team`은 1개 생성된다. 어떤 메서드의 `new`가 다시 실행되는지 호출 경로로 추적해야 한다. | 2026-08-01 |
| 2026-07-29 | Prototype을 두 번 생성하면 컨텍스트 종료 시 `@PreDestroy`도 두 번 호출될 것으로 예측함 | Prototype 두 개를 조회한 뒤 컨텍스트를 닫아도 소멸 카운터가 0임을 검증함 | 컨테이너는 Prototype의 생성·의존관계 설정·초기화까지만 담당하고 전달 후에는 추적하지 않으므로 소멸 콜백을 자동 호출하지 않는다. | 2026-08-01 |
| 2026-07-29 | 기본 Singleton도 명시적으로 조회할 때 생성된다고 설명함 | 컨텍스트 생성 직후, 첫 `getBean()` 전에 Singleton 생성 카운터가 이미 1임을 검증함 | 기본 non-lazy Singleton은 컨텍스트 초기화 중 미리 생성된다. 조회 시 생성되는 `@Lazy` Singleton과 구분해야 한다. | 2026-08-01 |
| 2026-07-29 | 숫자 카운터의 값 비교에 `assertSame`을 사용함 | 객체 참조 검증과 정수 값 검증을 분리하고 `assertEquals`로 생성·소멸 횟수를 확인함 | `assertSame`은 객체 참조 동일성, `assertEquals`는 값 동등성 검증에 사용한다. 작은 `Integer` 캐시 때문에 잘못된 참조 검증이 우연히 통과할 수 있다. | 2026-08-01 |
| 2026-07-29 | Singleton 객체가 생성부터 소멸까지 자신의 생명주기를 스스로 책임진다고 표현함 | 컨텍스트가 Singleton을 저장·추적하고 `close()` 시 `@PreDestroy`를 호출하는 흐름을 관찰함 | Singleton 인스턴스를 생성·저장·추적하고 소멸 콜백을 호출하는 책임 주체는 객체 자신이 아니라 Spring 컨테이너다. | 2026-08-01 |
| 2026-07-29 | 의존 객체가 없으면 서비스 생성자 안에서 실패한다고 설명함 | BeanFactory가 생성자 또는 팩토리 메서드를 호출하기 전에 의존 후보를 검색하는 순서를 다시 추적함 | 의존 BeanDefinition이 없으면 의존성 해결 단계에서 실패하므로 서비스 생성자와 이후 초기화·저장은 실행되지 않는다. | 2026-08-01 |
| 2026-07-29 | 설정 처리기가 등록 시점에 실제 의존 객체 참조까지 연결한다고 설명함 | BeanDefinition 등록 여부와 컨텍스트 초기화 중 의존 Bean 우선 생성·주입 이벤트를 분리해 검증함 | 등록 단계에서는 타입·팩토리 메서드·Scope 같은 메타데이터를 기록하고, 실제 후보 선택과 객체 참조 연결은 Bean 생성 시점에 BeanFactory가 수행한다. | 2026-08-01 |
| 2026-07-29 | `ReportRepository`를 인자로 전달해 다시 `ReportRepository`를 생성한다고 설명함 | `reportRepository()`와 `reportService(ReportRepository)`의 생성 이벤트 및 참조 동일성을 검증함 | BeanFactory가 관리 중인 `ReportRepository` 참조를 팩토리 메서드에 전달하고, 그 참조를 사용해 새로 생성되는 객체는 `ReportService`다. | 2026-08-01 |
| 2026-07-30 | Controller 반환값이 Java 바이트코드로 전송된다고 표현함 | 실제 HTTP 요청과 응답 본문을 전송하고 Java `HttpClient`가 받은 문자열을 검증함 | Java 바이트코드는 JVM이 실행하는 `.class` 명령이고, HTTP 본문은 전송 데이터를 인코딩한 바이트 표현이다. | 2026-08-02 |
| 2026-07-30 | 서버와 클라이언트의 객체가 다른 이유를 통신 코드 규격 차이로 설명함 | 실제 서버와 클라이언트가 독립된 실행 환경에서 HTTP 본문을 쓰고 읽는 경계를 추적함 | 객체 참조는 네트워크를 직접 통과하지 않는다. 독립된 메모리 공간 사이에는 데이터 표현만 전달되고 수신자가 자신의 값이나 객체를 새로 구성한다. | 2026-08-02 |
| 2026-07-30 | 같은 Servlet 객체가 요청을 처리하지만 생성자와 `init()`도 요청마다 호출된다고 예측함 | 두 요청 전후의 `constructor`·`init`·`service` 이벤트와 Servlet 식별값을 검증함 | 하나의 Servlet 인스턴스가 여러 요청을 처리하므로 생성자와 `init()`은 한 번이고 `service()`가 요청마다 호출된다. | 2026-08-02 |
| 2026-07-30 | Spring 처리기가 Servlet의 `init()`과 `service()`를 직접 호출한다고 설명함 | Servlet 클래스를 `ServletContext`에 등록하고 실제 생명주기 이벤트의 관리 주체를 분리해 추적함 | Servlet의 생성과 생명주기 메서드 호출을 직접 담당하는 주체는 Servlet Container인 Tomcat이다. | 2026-08-02 |
| 2026-07-30 | `ServletContextInitializer`가 Servlet 객체를 생성한다고 판단함 | Initializer에서 객체가 아닌 `LifecycleServlet.class`를 등록하고 생성자 이벤트가 Tomcat 시작 중 발생하는 것을 확인함 | Initializer는 Servlet 등록 정보를 제공하고, 등록된 클래스로 실제 Servlet 객체를 생성하는 주체는 Servlet Container다. | 2026-08-02 |
| 2026-07-30 | Servlet Container가 요청마다 서비스를 반환한다고 표현함 | 요청 2회마다 같은 Servlet 인스턴스의 `service(request, response)` 호출 이벤트가 한 번씩 추가되는 것을 검증함 | Servlet Container는 요청마다 같은 Servlet 인스턴스의 `service(request, response)`를 호출하고, Servlet이 응답 객체에 결과를 기록한다. | 2026-08-02 |
| 2026-07-30 | `destroy()`가 Servlet 객체를 직접 메모리에서 삭제한다고 생각함 | 컨텍스트 종료 시 `destroy` 이벤트와 JVM 객체 메모리 회수 책임을 분리해 분석함 | `destroy()`는 종료 시 자원 정리 기회를 주는 콜백이며, 실제 객체 메모리 회수는 이후 JVM GC가 담당한다. | 2026-08-02 |
| 2026-07-31 | Singleton이면 메서드 지역변수도 요청 사이에 공유될 수 있다고 생각함 | 두 동시 요청의 서로 다른 Tomcat 스레드명과 같은 Singleton Controller 식별값을 함께 검증함 | 메서드 지역변수는 호출별 스택 프레임에 분리된다. 인스턴스 필드는 공유되며, 분리된 지역변수도 같은 변경 가능 객체를 참조하면 그 객체의 상태는 공유된다. | 2026-08-05 |
| 2026-07-31 | A 요청이 공유 필드에 A를 저장했으니 응답 직전에도 A를 읽을 것으로 예측함 | `CountDownLatch`로 `A 저장 → B 덮어쓰기 → A 재개`를 고정하자 A와 B 모두 공유 필드에서 B를 읽음 | 요청 인자로 받은 원래 값과 Singleton 공유 필드에서 나중에 읽는 값은 다를 수 있다. 다른 요청 스레드가 중간에 필드를 덮어쓰면 요청 간 간섭이 발생한다. | 2026-08-05 |
| 2026-08-02 | `chain.doFilter()`가 전체 Filter 호출 사슬이 시작되는 지점이라고 설명함 | 현재 Filter는 이미 Tomcat에 의해 호출된 상태에서, `chain.doFilter()` 전후의 이벤트와 Controller 이벤트를 비교함 | `chain.doFilter()`는 남은 다음 Filter 또는 최종 Servlet을 호출하고 그 처리가 끝나면 복귀하는 중첩 메서드 호출이다. | 2026-08-05 |
| 2026-08-02 | Filter가 401로 차단해도 같은 Filter의 `return` 뒤 `filter-after`가 실행될 것으로 예측함 | `filter-before → filter-blocked → return`을 실행하자 실제 이벤트는 `request-initialized → filter-before → filter-blocked → request-destroyed`였음 | 같은 Filter에서 `return` 뒤의 후처리는 실행되지 않는다. 다만 이 Filter를 `chain.doFilter()`로 호출한 상위 Filter가 있다면, 호출이 복귀한 뒤 상위 Filter의 후처리는 실행될 수 있다. | 2026-08-05 |
| 2026-08-02 | 실제 요청 처리를 위한 응답은 Filter 내부에서 생성된다고 설명함 | 정상 요청에서 Filter는 `chain.doFilter()`로 위임하고 Controller 이벤트가 실행됐으며, 차단 요청에서만 Filter가 401을 직접 설정함 | Filter는 요청·응답을 검사·변경하고 진행을 통제할 수 있다. 정상 요청에서는 Controller가 처리 결과를 반환하고 Spring MVC가 HTTP 응답에 기록하며, Filter는 필요할 때만 차단 응답을 직접 작성할 수 있다. | 2026-08-05 |
| 2026-08-02 | `ServletRequestListener`가 요청 Servlet의 생성과 소멸을 관찰한다고 설명함 | 같은 Servlet 인스턴스가 여러 요청을 처리하는 동안 요청마다 `requestInitialized`와 `requestDestroyed`가 각각 발생한 기존 실험을 다시 비교함 | `ServletRequestListener`는 Servlet 객체의 생성·소멸이 아니라 요청 객체의 생명주기 시작·종료를 관찰한다. Servlet의 `init()`·`destroy()` 생명주기와 구분해야 한다. | 2026-08-05 |
| 2026-08-03 | HandlerMapping이 HandlerAdapter를 반환하고 Handler와 HandlerAdapter가 같은 대상이라고 설명함 | `handlersByPath`에서 `MethodStyleHandler` 또는 `DirectStyleHandler`를 꺼내 `HandlerExecutionChain`에 넣는 코드와 별도로 등록된 HandlerAdapter Bean을 비교함 | Handler는 요청을 실제로 처리하도록 선택된 대상이고, HandlerAdapter는 `supports(handler)`로 그 대상을 지원하는지 확인한 뒤 `handle()`로 호출하는 별도 협력 객체다. | 2026-08-06 |
| 2026-08-03 | HandlerAdapter의 `supports()` 검사와 `handle()` 실행을 같은 것으로 보고, 지원하지 않는 Method Adapter도 Direct Handler 경로의 이벤트에 기록될 것으로 예측함 | `/direct-handler`에서 Method Adapter의 `supports()`는 false이고 Direct Adapter만 선택되어 `direct-style-adapter → direct-style-handler`가 기록되는 assertion을 실행함 | 여러 Adapter의 `supports()`가 후보 검사에 사용될 수 있지만 실제 `handle()`은 지원하는 Adapter 하나에서 실행된다. 현재 이벤트는 `handle()`만 기록하므로 검사와 실제 호출을 분리해 해석해야 한다. | 2026-08-06 |
| 2026-08-04 | HttpMessageConverter가 HTTP 전체 문자열을 Java 객체로 변환한다고 생각함 | 경로 변수·요청 파라미터의 타입 변환과 `@RequestBody` JSON의 DTO 변환 경로를 분리해 검증함 | HttpMessageConverter는 HTTP 전체가 아니라 요청·응답 본문과 Java 객체 사이를 변환한다. `@PathVariable`과 `@RequestParam`의 문자열 기반 변환은 별도 인자 해석·타입 변환 경로를 사용한다. | 2026-08-06 |
| 2026-08-04 | Handler를 찾은 뒤 필수 요청 파라미터가 누락된 경우도 404가 반환된다고 생각함 | 매핑된 Controller에 `urgent` 파라미터 없이 요청하자 400이 반환되고 Controller 호출 횟수는 0이었음 | 404는 Handler를 찾지 못한 경우이고, 필수 요청 파라미터 누락은 Handler 선택 이후 Controller 인자 준비 단계의 실패다. | 2026-08-06 |
| 2026-08-06 | 잘못된 입력으로 발생한 실패를 모두 401로 판단함 | 경로 변수 타입 불일치와 DTO 검증 위반 요청은 인증 처리와 무관하며 각각 400 응답으로 검증됨 | 401은 인증이 필요하거나 인증에 실패한 경우에 사용한다. 요청값의 타입 변환·검증 실패는 400으로 구분해야 한다. | 2026-08-09 |
| 2026-08-06 | Bean Validation 실패는 DTO 생성 자체가 실패한 것이라고 생각함 | 정상 JSON이 `CreateOrderRequest("", 0)`으로 변환된 뒤 `@NotBlank`·`@Min` 검증에서 실패하고 Controller 호출 횟수는 0이었음 | JSON 본문을 DTO로 만드는 변환과 생성된 DTO의 제약조건을 검사하는 검증은 별도 단계다. | 2026-08-09 |
| 2026-08-06 | `/orders/abc`의 타입 변환 실패를 HandlerMapping의 경로 매핑 실패로 생각함 | `/orders/{id}` Handler가 선택된 뒤 ArgumentResolver가 `"abc"`를 `long`으로 변환하면서 예외가 발생함 | 경로 패턴 일치와 Controller 인자 타입 변환은 별도 단계이며, 이 경우 Controller 호출 전 인자 준비 단계에서 실패한다. | 2026-08-09 |
| 2026-08-06 | 정상 URL과 검증을 통과한 DTO라면 반드시 200 응답이 나온다고 생각함 | DTO 검증을 통과한 요청이 Controller에 진입한 뒤 재고 없음 조건으로 `SoldOutException`을 발생시켜 409로 변환됨 | 요청 형식과 Bean Validation을 통과해도 비즈니스 규칙 위반이 발생할 수 있으며, 전역 예외 처리기가 도메인 실패에 맞는 응답으로 변환할 수 있다. | 2026-08-09 |
| 2026-08-09 | Controller에서 `SoldOutException`이 발생했으므로 `afterCompletion`도 그 예외를 인자로 받을 것으로 예측함 | `@RestControllerAdvice`가 예외를 409 응답으로 해결한 경로에서 `interceptor-afterCompletion:null`이 기록됨 | `afterCompletion`의 예외 인자는 발생 이력이 아니라 MVC가 최종적으로 해결하지 못한 예외다. 처리된 예외 경로에서는 `null`을 받는다. | 2026-08-12 |
| 2026-08-09 | `@RestControllerAdvice`가 AOP의 `after-throwing`보다 먼저 Controller 예외를 처리한다고 예측함 | 이벤트는 `controller → aop-after-throwing → aop-finally → advice-sold-out` 순서로 기록됨 | Controller의 직접 호출자인 AOP 프록시가 먼저 예외를 관찰하고 재전파한 뒤, DispatcherServlet의 예외 처리기가 Advice를 호출한다. | 2026-08-12 |
| 2026-08-09 | `filter-after`는 `chain.doFilter()` 내부 결과와 관계없이 항상 실행된다고 설명함 | Filter가 `chain.doFilter()` 전에 예외를 던진 경로에서는 `filter-before → filter-throwing`만 기록됨 | 일반 후처리 코드는 `chain.doFilter()`가 정상 복귀해야 실행된다. 예외 여부와 무관하게 실행하려면 `finally`에 둬야 한다. | 2026-08-12 |
| 2026-08-09 | AOP를 Controller 메서드 전후에만 적용되는 경계로 설명함 | 같은 방식의 프록시 경계가 HTTP 호출과 배치 호출이 공유하는 Service Bean 메서드에도 적용될 수 있음을 호출 경로로 비교함 | AOP는 Controller에 한정되지 않고 포인트컷과 일치하며 프록시를 통과한 Spring Bean 메서드 호출에 적용된다. | 2026-08-12 |
| 2026-08-09 | JDK 프록시와 CGLIB 프록시의 런타임 클래스가 각각 원본 클래스와 같을 것으로 예측함 | 두 프록시의 `getClass()`와 원본의 `getClass()`를 비교하자 모두 달랐음 | 두 방식 모두 원본과 별도의 런타임 프록시 클래스를 만든다. JDK 프록시는 인터페이스를 구현하고 CGLIB 프록시는 구현 클래스를 상속한다. | 2026-08-12 |
| 2026-08-09 | CGLIB 프록시를 구현 클래스 타입으로 캐스팅하면 다른 객체 참조가 될 것으로 예측함 | 캐스팅 전후 참조를 `==`로 비교하자 같은 CGLIB 프록시 객체였음 | 캐스팅은 새 객체를 만들지 않고 기존 객체를 해당 타입으로 볼 수 있는지 검사한다. | 2026-08-12 |
| 2026-08-09 | `final` 클래스도 CGLIB 프록시 생성이 성공할 것으로 예측함 | `final` 클래스를 대상으로 `factory.getProxy()`를 호출하자 프록시 생성 단계에서 실패함 | CGLIB는 대상 클래스의 하위 클래스를 생성하므로 `final class`는 프록시를 만들 수 없다. `final method`는 프록시는 만들 수 있지만 재정의할 수 없어 Advice가 적용되지 않는다. | 2026-08-12 |
| 2026-08-09 | Advice 이벤트 목록에 target의 반환값인 `"paid"`·`"blocked"`도 기록된다고 생각함 | 반환값 assertion과 `events.add(...)`로 기록한 이벤트 assertion을 분리해 비교함 | 반환값은 호출자에게 전달되는 값이고 이벤트 목록에는 명시적으로 `events.add(...)`한 값만 들어간다. 호출 순서와 반환값을 분리해 추적해야 한다. | 2026-08-12 |
| 2026-08-09 | self-invocation에서 Advice가 적용되지 않는 이유를 `outer()`가 Pointcut 대상이 아니기 때문이라고 설명하고, Advice가 우회되면 내부 target 메서드도 실행되지 않는다고 예상함 | `proxy.inner()`와 `proxy.outer()`를 비교하자 전자는 Advice 전후 이벤트가 기록되고 후자는 `target-outer → target-inner`만 기록됨 | 핵심 원인은 `target.outer()` 내부의 `this.inner()`가 `target → target`으로 호출되어 프록시를 다시 통과하지 않는 것이다. Advice만 우회되며 원본 `inner()` 비즈니스 로직은 실행된다. | 2026-08-12 |
| 2026-08-09 | 트랜잭션 시작을 요청하고 실제로 처리하는 주체를 모두 프록시 또는 막연히 Spring이라고 설명함 | 프록시 Bean 조회부터 `TransactionInterceptor`, `DataSourceTransactionManager`, target의 활성 상태를 순서대로 추적함 | 프록시는 호출을 가로채고, `TransactionInterceptor`가 시작을 요청하며, `DataSourceTransactionManager`가 Connection으로 실제 JDBC 트랜잭션 시작과 스레드 자원 연결을 처리한다. | 2026-08-12 |
| 2026-08-09 | 새 스레드가 기존 트랜잭션 Connection을 얻지 못하는 직접 이유를 원자성 침해 때문이라고 설명함 | 기존 스레드와 새 스레드의 `TransactionSynchronizationManager` 자원 연결 상태를 분리해 비교함 | Spring 로컬 트랜잭션의 Connection 연결 정보는 현재 스레드에 한정되며 새 스레드로 자동 전파되지 않는다. 같은 Connection의 동시 사용 위험은 별도 문제다. | 2026-08-12 |
| 2026-08-10 | 예외 경로는 나중에 rollback되므로 flush 직후 같은 Connection에서도 행 개수가 0일 것으로 예측함 | `persist → flush → RuntimeException` 경로에서 rollback 전 같은 Connection의 행 개수는 1이고 완료 후 최종 행 개수는 0이었음 | rollback은 SQL이 실행되지 않은 과거로 되돌리는 것이 아니라, 현재 트랜잭션에서 실행된 미완료 변경을 최종 반영하지 않고 폐기한다. | 2026-08-13 |
| 2026-08-10 | flush 뒤 별도 Connection에서 행이 보이지 않는 이유를 별도 스레드이기 때문이라고 설명함 | 같은 스레드에서 별도의 물리 Connection을 얻고 `READ_COMMITTED`로 조회해도 미커밋 행은 보이지 않았음 | 직접 원인은 스레드 차이가 아니라 트랜잭션과 격리 수준이다. `READ_COMMITTED`에서는 다른 트랜잭션의 미커밋 변경을 읽을 수 없다. | 2026-08-13 |
| 2026-08-10 | `persist()` 직후 `EntityManager.find()`가 엔티티를 반환하면 INSERT SQL 실행이 증명된다고 생각함 | 1차 캐시가 같은 엔티티를 반환할 수 있어, flush 전후 같은 DB Connection의 행 개수를 별도로 비교함 | 영속성 컨텍스트 조회 결과와 DB SQL 실행 여부는 분리해야 한다. SQL 실행 시점은 DB Connection을 통한 관찰이나 SQL 기록으로 검증한다. | 2026-08-13 |
